if two functions are continuous, their sum, product, quotient, composition are also continuous

if f continuous for interval (a,b):
f takes on any given value between f(a) and f(b)

inverse function of continuous sms/smf function is also continuous sms/smf