`\documentclass`, z.B.
- article
- book

usepackage
Nützliche Packages
- amsmath
- amsfonts


title, author, date

{notwendig}
\[optional\]

```
\begin{z.B. document}
environment
\end{z.B. document}
```

`\cite{}` referenced biblio.bib für citations (Format BibTex)

Inline `\( \)`
Display `\[ \]` oder `\begin{equation} \end{equation}`

`\\`für newline (nicht absatz)

`\left` und `\right`für groß genuge Klammern

`&`vor `=`s um sie zu alignen

Commands für Whitespace (`quad`,`qquad`, etc.)
