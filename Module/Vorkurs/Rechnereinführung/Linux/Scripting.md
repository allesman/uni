```
#!/pfad_zum_interpreter
commands
```

Dateiendung ist Betriebssystem egal, aber bei Shell-Skripten `.sh` üblich
Zugriff auf Variable mit `$Identifier` bzw `${Identifier}`
`read`liest input
`if x ;then ... fi`
`for x in y ; do ... done`
