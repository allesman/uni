für immer kleiner werdendes h, schrumpft $f(x+h)-f(x)$ schneller

$f'(x) :=(\frac{d}{dx}f)(x)$
$f''(x) :=(\frac{d^{2}}{dx^{2}}f)(x)$
$(\frac{d^{n}}{dx^{n}}f)(x)=f^{(n)}$

if differentiable -> continuous

![[Pasted image 20250923170525.png]]
$\frac{d}{dx} b^{x} = \ln(b)\cdot b^{x}$ for $b>0$
normales vorschieben und exponent minus ableiten gilt für Exponent $e \in \mathbb{Z} \cup \mathbb{R}_{>0}$
