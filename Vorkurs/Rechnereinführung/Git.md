vcs etc.
`git switch -c branch`um neuen branch zu erstellen
merge vs rebase

`git pull`ist einfach nur `git fetch`und dann `git merge origin/branch`
`git push`geht nur wenn das für remote fast-forward wäre -> vorher pull löst meistens das problem
