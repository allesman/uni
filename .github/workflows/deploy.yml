name: Deploy Obsidian Export

on:
  push:
    branches:
      - main
    paths:
      - 'HTML Exports/**'
  workflow_dispatch:

jobs:
  deploy:
    name: Deploy 🚀
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Prepare Files and Hide Extensions
        run: |
          cd "HTML Exports"
          # 1. Identify the content file
          target_file=$(ls einf*hrung*.html 2>/dev/null | head -n 1)
          
          if [ -n "$target_file" ]; then
            echo "Patching metadata and hiding extensions..."
            
            # 2. Update the internal pathname to 'eidi' (without .html)
            sed -i 's/meta name="pathname" content="[^"]*"/meta name="pathname" content="eidi"/g' "$target_file"
            
            # 3. Create .htaccess to hide the .html extension on the server
            # This allows the browser to load 'eidi' instead of 'eidi.html'
            cat <<EOF > .htaccess
          RewriteEngine On
          RewriteCond %{REQUEST_FILENAME} !-f
          RewriteCond %{REQUEST_FILENAME}.html -f
          RewriteRule ^(.*)$ \$1.html [L]
          EOF

            # 4. Generate the redirect index pointing to 'eidi' (no extension)
            cat <<EOF > index.html
          <!DOCTYPE html>
          <html>
            <head>
              <meta charset="UTF-8">
              <meta http-equiv="refresh" content="0; url='eidi'" />
              <title>Einführung in die Informatik</title>
            </head>
            <body>
              <p>Redirecting...</p>
            </body>
          </html>
          EOF
        fi
          
      - name: Sync files to hosting
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: v132927.goserver.host
          username: allesman
          password: ${{ secrets.FTP_PASSWORD }}
          local-dir: ./HTML Exports/
          server-dir: ./eidi/
          delete-missing: true
